image: python:3.10

stages:
  - test
  - deploy

unit-tests:
  stage: test
  script:
    - ./make ci_test

pages:
  stage: deploy
  needs:
    - unit-tests
  script:
    - pip install -r requirements.txt
    - ./make shell_docs_build
    - mv site public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

